## 前提

- このアプリは私的利用の範囲内である。不特定多数に公開する前提で作られていない。

## クリップデータの作成手順

YouTube URL（または videoId）を渡されてクリップデータを作る場合、以下の手順で実データを作る。

1. 対象の `videoId` を確認する。基本的にクリップ単体の動画（元配信全体ではない）を指すので、top-level `start`/`end` は0〜動画全体の長さ、`lines` 内の `start`/`end` も動画先頭(0秒)からの秒数でよい。
2. ブラウザツールで対象タブを開き、`https://www.youtube.com/watch?v={videoId}` にアクセスして「文字起こしを表示」(Show transcript) から字幕テキストとタイムスタンプを取得する。複数タブ・複数エージェントで並行する場合は必ず専用タブ（`tabId`）を使い分ける。手順は以下の通り（動画再生バーの「字幕を利用できません」表示は文字起こしの有無と関係ないため無視してよい）。
   1. 動画ページに遷移したら `k` キーで一時停止する（短い動画だと自動再生で次の動画に移ってしまうため）。
   2. スクリーンショットを撮り、概要欄のスニペット（再生回数・タグの行、「...もっと見る」の直前）をピクセル座標でクリックして概要欄を展開する。**refベースのクリックはこのページでは古い状態を参照して失敗することがあるため、直前に撮ったスクリーンショットの座標でクリックすること。** 展開できたかは再度スクリーンショットで確認する（クレジット等の全文が見えていればOK）。
   3. 概要欄内を下にスクロールし、「文字起こし」見出しの下にある「文字起こしを表示」ボタンを探してクリックする。
   4. `get_page_text` でタイムスタンプ付きの文字起こし全文を取得する。
3. **文字起こしがどうしても取得できない動画は内容を捏造しない。** その場合はスタブ（`lines` に `{"start":0,"end":1,"en":"TODO","ja":"TODO"}` の1件のみ、`"memo":"stub - needs transcription"` 付き）のまま残し、取得不可として報告する。
4. 取得した文字起こしを自然な発話単位で `lines` に分割し、各行に以下を入れる。
   - `en`: フィラーや `[Music]` 等のタグを除去し整形した英語テキスト
   - `ja`: 直訳ではなく自然な口語の日本語訳（生成でよい）
   - `notes`（任意）: イディオム・スラング・聞き馴染みのない言い回しや単語がある行にのみ追加。`term`/`meaning`（省略時表記が異なるなら `match` も）。多用しすぎない
   - スタイルは既存の完成済みファイル（例: `src/data/clips/5-3.json`）を手本にする
5. スタブでなくなったら `"memo"` フィールドは削除する。
6. `npm run check:data` でスキーマ検証を行い、エラーがあれば修正する。
